import React from 'react';
import { Meta, Story } from '@storybook/react';
import { useButtonDown } from '../src';

export default {
  title: 'Welcome',
};

export const Default = () => <div>hey</div>;
